## Context

迁移所在工作树包含其他开发中的代码和未跟踪审计文件。文件在本地存在不等于迁移独立交付时可访问。

## Decisions

保留三份用户原始审计，索引使用路径说明明确其未跟踪状态，不建立随迁移发布的链接。复查主规范时分别验证格式、来源存在、来源哈希与实现语义；这些检查不互相替代。测试使用现有 chat-state 与 workflow 套件，不编写镜像测试。

## Risks / Trade-offs

构建目录可能被其他任务持有。若测试未能执行，明确记录，不能将等待锁算成测试通过。不为本次文档验证抢占其他任务或触发全仓重构。
