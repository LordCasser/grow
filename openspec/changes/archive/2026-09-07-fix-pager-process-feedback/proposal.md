## Why
外部分页器调用丢弃 Command::status 的全部结果。可执行程序不存在、无法启动或非零退出时，用户只看到界面恢复，没有错误原因，容易把配置错误误认为 transcript 命令没有执行。

## What Changes
保留进程结果，在终端恢复后通过当前模式可见的通知显示启动错误或失败退出状态；成功不新增提示。

## Impact
仅 Pager 外部分页器调用和反馈，不改变命令解析、参数、同步子进程等待、超时重试或临时文件清理。
