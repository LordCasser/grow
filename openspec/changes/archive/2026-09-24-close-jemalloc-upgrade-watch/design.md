# Design

Keep the existing archived feasibility audit as the durable record. Remove the passive watch entry from the active backlog; if upstream publishes a fix or Grow later validates a candidate on both musl release targets, assess the upgrade as new work with current evidence.

No source, dependency, lockfile, or accepted specification changes are in scope.
