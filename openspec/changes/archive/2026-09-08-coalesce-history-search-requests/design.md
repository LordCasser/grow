# Design
使用一个私有待处理消息槽和容量1的唤醒通知，保留最新状态；将既有drain_to_latest中的合并规则用于提交时合并，确保SetItems之后SetQuery不丢items。worker取走待处理请求后锁外匹配和发布。停止必须压过待处理消息且不等待容量，不join仍在匹配的线程。

不直接对原消息try_send后丢弃：那会丢失最后输入。测试使用可控暂停worker或未启动receiver，提交超过旧容量，证明返回并保留最后items/query；验证drop不阻塞、已有组合/搜索/浏览测试以及相关调用方回归。具体私有结构在实施时按周边代码最小化，不引入通用任务框架。
