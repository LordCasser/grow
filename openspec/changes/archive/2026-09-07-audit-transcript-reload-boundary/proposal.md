## Why
分页器请求跨视图执行需要核对实际所有权。不能因为请求只存 AgentId 就推断会话串线，也不能忽略重连暂存正文对分帧输出的影响。本项以实际加载、清空及重连代码确定下一修复边界。

## What Changes
记录源代码审计：普通标签切换受 AgentId 绑定保护，EntryId 不重用；重连暂存会导致分帧构建跳过尚未渲染的旧条目。建立独立修复条目，不改运行时契约，跳过 delta specs。

## Impact
仅审计记录与 backlog，无源码或依赖改动。
