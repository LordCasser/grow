## Why
The user requests a patch milestone release after approved cleanup, preserving separate tested removal commits.

## What Changes
Bump workspace package metadata to 2.1.5 and publish concise release notes through the existing official workflow. This packaging change introduces no new product behavior; existing archived changes own the contracts.
