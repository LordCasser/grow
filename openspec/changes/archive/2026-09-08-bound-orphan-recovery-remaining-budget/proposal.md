## Why
占位图片恢复按per_image_max读取完整候选，再检查aggregate_max。剩余额度不足时仍会分配完整单图数据。
## What Changes
每个待恢复文件使用min(per_image_max, aggregate剩余额度)作为实际读取预算；余额为0立即停止。当剩余额度是更紧的限制且候选超限时停止剩余恢复；单图上限失败仍可跳过继续。
## Impact
恢复循环及预算测试；保持已有附图去重和图片编号，不把不同编号同路径的引用贸然合并。aggregate只计算本次新恢复字节，保持已有范围。
