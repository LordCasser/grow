## Decision
保留 usize 解析，错误分支先检查去掉单个可选 + 或 - 后是否为非空 ASCII 数字串。若是，按无效索引处理；其他输入继续作为文件路径。零索引沿用拒绝语义，用法提示增加数字文件名的 ./ 写法。

## Verification
纯 parser 测试覆盖负数、0、正负超范围值及尾随目标；正数、+1、带数字扩展名及显式 ./数字路径继续成功。无需真实文件写入或剪贴板。
