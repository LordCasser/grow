## Why
The user explicitly requested cleanup of R1–R11 before the milestone release. R1 is retained; R2/R3/R10 are approved; other entries require confirming reachability/replacement, especially external configuration initialization for R7.

## What Changes
Remove approved unused surfaces after rechecking consumers, preserve active implementations and record per-item decisions. R12 onward remain unapproved. Release resumes only after cleanup validation.
