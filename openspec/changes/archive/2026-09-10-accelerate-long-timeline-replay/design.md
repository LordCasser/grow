Bulk restoration owns a fresh Timeline that is never published until every event validates. It can validate event content against the preceding state and apply lifecycle transitions in place, instead of cloning accumulated historical maps for every event. Any error discards the entire private fold. Public prepare/accept keep transactional semantics for existing live state. Share event validation and projection application; preserve prior turn/step flags for delayed Control activation. Compare replay with transactional acceptance, malformed histories and a frozen real-session prefix. No live session is resumed by the benchmark or modified.

Content validation runs against the preceding fold before in-place lifecycle admission. For an event invalid in both categories, replay may report the content error before the lifecycle error; both are still rejected. Live admission keeps its existing diagnostic order and atomicity. Projection application captures the preceding active turn/step flags, because Control activation must not observe an advanced lifecycle.
