## Why
托管配置仅在读取前检查 metadata.len，之后 fs::read 没有限制。文件在两步间增长仍可能触发无界内存分配；发布验证与回滚验证也会完整读取变化后的文件。

## What Changes
源读取最多读取4 MiB加一个超限探测字节；验证读取最多读取计划长度加一个字节，超限返回错误。

## Impact
config managed_text 的读取和验证；不扩大可接受配置大小。
