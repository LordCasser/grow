## Design
复用一个接受 Read 的内部有限读取函数，take(limit+1) 后检查实际长度。源读取保留已有元数据预检，但不依赖预检确保预算；验证按已知计划长度设置预算，超限进入既有错误/冲突处理。避免把计划输出强制限制为4 MiB而改变现有生成行为。

## Scope
不在此变更解决 metadata 与 open 的文件身份竞态或慢文件系统总期限；这些需独立审计。只承诺读取字节上限，不承诺调用总超时。

## Verification
计数读取器验证实际最多读取 limit+1，覆盖0、恰好上限及超限；托管配置完整回归和发布后超大外部文件保持。
