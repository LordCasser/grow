## Verification

- `rustfmt --edition 2024 crates/codegen/pager/tests/pty_e2e/resume_input_latency.rs crates/codegen/pager/tests/pty_e2e_persistence.rs` — passed.
- `openspec validate add-dense-tool-resume-pty-probe --strict --no-interactive` — passed.
- Focused dense PTY run (executed by the parent agent) completed `session.load_session` in 2038 ms and replay in 625 ms. The first streamed marker appeared at 2001 ms, at the edge of load completion, and the final marker appeared during key 1 because the test waited for the streamed marker before starting input. This was a test-window problem; it did not demonstrate slow key echo.
- The dense case now starts input on the visible `Loading session` prompt, before waiting for the streamed marker. It tracks the first marker time, samples all 20 echoes while checking final-marker absence, prints phase timings before the assertion, and retains draft-survival verification.
- `CARGO_INCREMENTAL=0 CARGO_PROFILE_TEST_DEBUG=0 CARGO_PROFILE_DEV_DEBUG=0 CARGO_BUILD_JOBS=2 PAGER_BINARY=$PWD/target/debug/grow cargo test --locked --offline -p pager --test pty_e2e_persistence resume_input_during_dense_tool_replay_with_slow_writer -- --ignored --nocapture --test-threads=1` — passed. The 512-turn, 31,097,749-byte interleaved history held the final replay marker until after all 20 key echoes; p95 was 52.9 ms, maximum 58.5 ms, and the draft survived SessionLoaded. The first streamed marker was not visible during the echo window, so this run measures loading-period input responsiveness rather than post-stream-marker repaint.
