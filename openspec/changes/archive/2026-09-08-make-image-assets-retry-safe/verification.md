# Verification
- main. Low-disk locked/offline shell lib image_describe::tests: final21 passed,0 failed/ignored,0.12s. Incremental off,dev/test debug=0,jobs=2,RUST_MIN_STACK=16777216.
- First run20 passed/1 failed because the existing image_files test asserted the old flat UUID layout. Updated it to verify that the exact real published file path appears in the message; full rerun passed.
- Retry test prohibits any writer callback after first publication and requires identical paths. Injected failure in a new two-image batch preserves the previous batch; corruption of existing bytes is rejected without overwrite/removal. Two threads barrier-synchronize private staging writes and return the same batch with one published directory and no leftover stage. Existing empty/order/symlink rejection tests pass.
- No real user home or installed binary changes. All IO cases use explicit temporary contained directories. Existing admission still does not delete assets on commit errors; source inspection establishes ACK-loss retention, not a new Timeline ACK-loss integration test.
- No Windows execution, process-kill recovery/GC or power-loss durability test. A post-publication parent-sync failure warns; crash/failed stage creation/cleanup may leave staging. Distinct ordered batches may duplicate individual images. Already published unique batches are not garbage collected by this change.
