## Decision

Keep the compact production projection API. The test converts `Assistant` and `Reasoning` chunks to their corresponding ACP update variants when comparing the old load-all reference with streaming replay.
