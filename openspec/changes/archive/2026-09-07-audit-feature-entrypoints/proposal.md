# Why
连续审计需要区分默认关闭、已有执行路径和真正未接入的功能，避免将配置开关误判为删除理由，也避免一直停留在 web_fetch。此次核对一批配置入口及生产消费者，形成下一阶段的证据入口。

# What Changes
只记录当前功能入口与未验证范围，不修改行为、配置或测试。

# Impact
审计记录仅在此 change 内；三个现有删除候选继续等待用户确认。skip_specs 因无行为契约变化。
