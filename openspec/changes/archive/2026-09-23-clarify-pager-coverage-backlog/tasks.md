# Tasks

- [x] 1.1 Confirm ignored Pager coverage test names and owning integration targets.
- [x] 1.2 Narrow only the two Pager bullets in `openspec/backlog.md` and record the documentation-only scope.
- [x] 2.1 Run `git diff --check` and `openspec validate --all --strict --no-interactive`.
