# Tasks

- [x] Add the selected-view and frame-owner seam; align stale-child input and Minimal key routing.
- [x] Route Minimal commit, viewport, live, plan and expansion through the selected view; rebase the native frontier when owner changes.
- [x] Test distinct parent/child content, switching back, permission precedence, child invalidation and failure/retry boundaries.
- [x] Update developer explanation, record verification, run targeted Rust and strict OpenSpec checks, then archive and remove the backlog entry.
