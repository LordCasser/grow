Audit ownership and transition boundaries first, then error/cancellation paths, replay and presentation. For each area record exact inspected paths, invariant, finding and validation; passing tests alone do not establish complete coverage. Use deterministic local/mock regressions, no live provider needed by default. Preserve user session files. Explicitly distinguish no token budget from finite budget, model context window and external provider quota.
