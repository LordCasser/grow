Completed, structurally identified Responses function calls with invalid JSON produce InvalidToolArguments. This typed error survives stream/error reconstruction. The sampler may retry despite observed preview output because no tool calls or native continuation have been admitted. Reuse the ordinary attempt counter and backoff, cap this recovery at three total attempts (or the configured lower cap); explicit max_retries=0 disables it. No client rebuild is needed for model output. Evidence and usage ACK precede each retry; cancellation, persistence failure and Goal budget closure retain authority. Successful Completed alone publishes executable calls/native continuation. On exhaustion the normal session error path pauses Goal. Other serialization/status/identity errors are unchanged.

The existing TUI deliberately projects automatic Retrying as ordinary running activity; preserve that behavior. Retry reason/attempt remain in structured notifications and diagnostics. No additional toast or prompt is needed.
