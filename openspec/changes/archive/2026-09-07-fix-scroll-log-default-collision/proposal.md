## Why
默认滚动日志用秒级时间戳命名，写入时 File::create 会截断已有文件。同秒快速关闭重启记录器，或多个实例同时记录，可能覆盖前一份诊断数据。默认生成路径需要独立身份。

## What Changes
默认文件名追加 UUID，保持首条记录才创建文件；同一生成器覆盖环境默认值和运行时开关。

## Impact
仅默认路径命名，不改 JSONL 格式、显式指定路径、记录节奏或滚动状态机。
