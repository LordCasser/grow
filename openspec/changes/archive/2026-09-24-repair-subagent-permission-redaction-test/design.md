## Decision

Keep the existing ACP handler test and its event fixture. Assert the safe Bash summary in compact and detail projections, then check both projections omit the command, its raw detail suffix, the supplied decision reason, and classifier prose. No production code or specification changes are needed.
