# Verification

- `cargo test -p pager --lib app::acp_handler::tests::subagents::subagent_permission_decision_renders_in_parent_scrollback_only`: passed (1 test).
- `openspec validate --all --strict --no-interactive`: passed (18 items).
