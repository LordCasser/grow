# Design
权限管理的预判与后判均使用 DomainMatcher 决定静态允许列表授予。先将配置按首个斜杠拆为主机和路径，再对主机 trim/lowercase/www/尾点处理。路径只去尾斜杠，按 URL.path 原始序列区分大小写，保留段边界要求。替换旧的路径忽略大小写测试，因为它锁定了错误的授权语义；加入末尾点和带路径的大写 WWW/主机尾点回归。

不把不匹配等同最终拒绝：它只是失去静态列表自动许可，后续仍由现有权限策略决定。重定向跨路径的授权传播另行审计。
