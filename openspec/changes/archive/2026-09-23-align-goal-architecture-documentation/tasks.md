## Tasks

- [x] Read the Goal article, `goal_tracker.rs`, the `behavior-goal` spec, and
      the backlog entry; confirm the drift is limited to documentation.
- [x] Update the article's version wording and durable state shape while
      preserving its lifecycle and accounting explanations.
- [x] Remove the resolved Goal documentation-drift entry from the backlog.
- [x] Run focused text checks and strict OpenSpec validation, record results,
      then archive and validate the archived change.
