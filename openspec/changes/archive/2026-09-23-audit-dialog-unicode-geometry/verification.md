# Verification

- `new_worktree_dialog::dialog_width_for` measures its fixed label and entry label with `UnicodeWidthStr`; render slices the input only at `SingleLineViewport::visible_byte_range` and places the cursor using `cursor_display_column`. Existing tests include `empty_dialog_uses_minimum_width`, `dialog_grows_with_long_label`, `dialog_clamps_to_terminal_width`, `long_name_fully_visible_on_wide_terminal`, `long_name_end_visible_on_narrow_terminal`, and `narrow_dialog_keeps_middle_unicode_cursor_visible`.
- Production single-line input paths in picker search, memory filtering, extension forms, dashboard rename/dispatch, and settings use UTF-8 byte ranges for slicing and display-column values for cursor placement. Existing targeted regressions include `picker_graphemes_paste_and_viewport_use_line_editor`, `viewport_search_bar_reserves_counter_without_text_or_cursor_overlap`, `filter_render_keeps_unicode_query_and_cursor_visible`, `narrow_form_viewport_keeps_unicode_and_cursor_visible`, dashboard `rename_viewport_handles_long_unicode_in_wide_and_narrow_rows`, and settings modal Unicode input coverage.
- Byte-counted search prefixes are fixed ASCII (`SEARCH_BAR_LABEL`, dashboard rename/search prefixes). `PickerField` production labels are fixed ASCII labels in session/welcome and extension detail builders; values may be Unicode and are wrapped by display width. Modal tab labels use Unicode display width. The modal close button width comes from five individually rendered one-column cells.
- No reachable production path was found where a non-ASCII header or label is measured in bytes and used as a terminal-column offset. No behavior change or Cargo build was required for this source audit.
