## Why
最近四项交互与诊断修复均已独立通过测试，当前 CLI 产物仍不包含这些改动。需要验证当前 main 工作树的组合根构建，提供可复查的实际二进制，而不只保留库测试结果。

## What Changes
按低磁盘配置构建 CLI，执行只读入口冒烟并记录产物身份。无行为契约改动，跳过 delta specs。

## Impact
包含首次历史加载保护、input dump 文件创建、scroll log 默认路径和失败状态修复；不替换用户已安装版本。
