# Verification
Scoped repository reads on main; no runtime changes, Cargo build, new benchmark or tests. Existing completed_image_sideband_is_recoverable_before_parent_projection test inspected, not rerun. Disk available 66 GiB and target already cleaned.
