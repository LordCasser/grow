# Why
Slash MRU 首次排名在 UI 线程用 fs::read 完整读取文件，再解析并裁剪256条。超大文件可造成无界读取，FIFO可阻塞菜单。现有条目上限无法约束读取阶段。

# What Changes
读取仅接受普通文件，先检查同一打开句柄的长度，再最多读取1 MiB加1字节，超限按读取失败处理。Unix用O_NONBLOCK避免打开FIFO等待写入者。读取失败沿用本会话禁用持久化，保护原文件。

# Impact
仅 slash MRU 加载路径；保留普通文件符号链接、缺失文件、损坏JSON和排名算法的现有行为。不改写队列、跨进程合并或异步加载架构。
