Confirmed wire format: a message is a dict with exactly `kind` (string), `seq` (nonnegative integer), and `body` (string). Encoder exposes `encode(kind, seq, body) -> dict` and `encode_batch(items) -> list[dict]`, where each item is a three-tuple. Client exposes `decode(message) -> tuple[str,int,str]` and `decode_batch(messages) -> list[tuple[str,int,str]]`. Reject malformed values with ValueError. Neither module imports the other.
