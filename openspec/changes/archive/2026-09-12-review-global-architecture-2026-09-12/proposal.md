## Why

用户要求依据 Grow 当前工作树进行全局软件架构审查，识别优化点并给出可分批批准的策略，明确同意后才实施。已有逐 crate 功能盘点不能代替跨模块所有权、依赖方向和恢复一致性审查。

## What Changes

- 核对主规范、现有 change、Cargo 依赖、关键实现及调用方和测试。
- 留存实际工作树基线、审查覆盖、代码证据和验证结果。
- 给出按风险与依赖排序的实施批次、边界及验收条件；登记待启动事项。
- 优先审查用户反馈的 session resume：历史出现后迟迟不能操作、完整加载慢；明确测量方案与磁盘预算。

## Capabilities

无新增或修改能力。本次只交付审计文档和独立探针证据，`skip_specs: true`；不修改产品 Rust 源码、Cargo 配置或产品行为，不为尚未批准的优化虚构 delta specs。将来的行为变更各自建立 change。

## Impact

仅本 change 的审计记录与 `openspec/backlog.md` 的待启动索引。审查包含开始前已有的未提交修改；保留其他 change 及其任务状态。完成并归档审计不代表优化获得实施授权。
