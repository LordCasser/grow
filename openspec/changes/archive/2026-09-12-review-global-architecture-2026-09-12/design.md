## Context

审查对象是 2026-09-12 当前工作树，HEAD 为 `8489761bda66848484c58d059f91dfa9bfbf98ed`，包含已有未提交代码。当前主规范提供行为边界；尚未归档的 `inventory-all-crate-features` 只作导航与待核对资料。

## Decisions

1. 从会话事实、执行调度、客户端投影、工具权限和基础依赖五个方向审查，优先确认跨层问题。
2. Atlas 只用于局部结构定位，Cargo metadata 和源代码用于复核，不声称拥有完整跨仓调用图。
3. 区分已确认缺口、架构债务、性能假设及已有修复；文件行数只作热点定位，包含测试的规模不等同生产复杂度。
4. 优化以现有状态所有者和 crate 为基础，先收敛依赖和接口，再处理必要的恢复契约，不提出全量重写。
5. 交付 `review.md`、`evidence.json`、`verification.md`，报告为后续批准提供明确批次。本次任务表仅表示审计完成情况。
6. 按用户补充把 session resume 列为第一批：区分历史首条可见、输入可交互和完整加载，核对 shell 重建、ACP replay 与 Pager 收尾；真实延迟未测量前不确定主瓶颈或承诺加速幅度。验证复用 target 并限制临时磁盘。

## Risks / Trade-offs

静态审查和本机验证不能证明所有 Provider、平台和崩溃窗口正确。已有未提交修改可能继续变化，证据记录读取版本及验证限制。性能优化需测量支撑，不承诺未测得的加速幅度。
