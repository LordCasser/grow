The regression uses the existing Pager `AgentView::clear_transport_interactions_for_session` teardown path. It asserts the pending ACP response is `Cancelled`, then dispatches the permission option that would enable always-approve. Because teardown removed the prompt, the stale action must produce no effects and leave the session in Ask mode. No implementation changes are expected.
