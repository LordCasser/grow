# Why
滚动诊断启用后持续写JSONL，没有大小上限，长时间记录可持续占用磁盘。

# What Changes
单记录器最多接受64MiB编码字节（含换行）；逐行预检查预算，达到或下一行会超出时刷出已有缓冲并禁用记录器。不开新文件轮转，不删历史日志。

# Impact
日志内容为完整行前缀，可能停在未finalize的手势内；group_streams已允许尾部未完成手势。普通滚动逻辑不读日志状态。不是日志目录总配额或慢盘超时。
