## Authority and tool surface
ask_parent is child-only. ask_subagent and send_subagent_message are parent operations restricted to an active directly-owned child; messaging upward is prohibited. Runtime resources bind sender identity. Immutable immediate-parent lineage, not lifecycle reparenting, authorizes routes. Workflow-owned hidden children retain their existing exclusion from manual parent control. Cross-session list_active_sessions/ask_session remain primary-only.

## Questions
Reuse InboundInquiry and SessionActor's asynchronous inquiry queue, frozen Timeline materialization, single tool-free InfoRequest Sideband, budget/usage settlement, durable InquiryEvent audit, and correlated Coordination UI projection. An authenticated local delegation route bypasses cross-workspace inquiry approval (worktree isolation is already authorized), without changing peer inquiry approval. The target foreground is neither interrupted nor given a synthetic user prompt. The caller's tool waits cancellably for the sideband result, while both actor mailboxes remain responsive; target timeout/closure is a typed local result. On the main-agent view a child question occupies one existing correlated inquiry row, updated through its lifecycle. No notification message/wait-reply protocol is added for questions.

## Parent intervention
Send only to a live direct child. The existing tool-call Timeline supplies source intent; a typed parent-message notification and immutable payload provide target durable receipt/dedup/consumption. Text is attributed to the delegating agent, never human input or a permission grant. Queued delivery joins context at the next safe sampling boundary. Immediate delivery uses the same soft-preemption boundary as user steering: cancel the active model request/interruptible wait, settle it, then resample within the child turn; non-interruptible tool side effects finish safely. No Goal pause, child cancellation, or second foreground owner is created. Pending received context remains recoverable.

## Verification
Cover role gates, direct/nested route ownership, unrelated/sibling/terminal rejection, asynchronous inquiry during foreground work, one receiving UI row, cancellation, sideband usage, queued versus immediate steering, durable replay/dedup and Goal/session invariants. Do not mutate live user sessions. Use reduced-debug builds and clean after testing.
