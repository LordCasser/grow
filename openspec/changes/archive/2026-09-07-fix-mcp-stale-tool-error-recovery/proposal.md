# Why
工具调用握住旧 McpService 等待响应时，另一路 recover 可以安装新服务。旧调用随后返回可恢复错误仍调用无身份的 recover，导致健康新服务被无谓重置。

# What Changes
工具错误恢复传递失败服务 Arc，只有当前 Ready 服务仍匹配时重置；若已替换则复用当前服务完成既有一次重试。

# Impact
仅可恢复工具错误分支；超时不重试分支独立处理，主动恢复仍沿用原入口。
