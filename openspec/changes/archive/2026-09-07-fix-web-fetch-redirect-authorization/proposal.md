# Why
权限管理只批准初始 URL，而 HTTP 客户端按主机相同自动跟随任意路径、端口和 scheme 跳转；路径限定或一次性许可因此被扩大。执行上下文没有携带可证明目标已授权的范围。

# What Changes
将现有跨主机的新调用提示推广到所有跳转，返回 RedirectRequired 和原始/目标 URL。每个目标通过新的工具调用重新经过现有权限入口；不在客户端复制权限策略。

# Impact
web_fetch 网络执行、输出枚举/提示、ACP 状态转换与测试。正常页面不变；重定向页面需新的工具调用。
