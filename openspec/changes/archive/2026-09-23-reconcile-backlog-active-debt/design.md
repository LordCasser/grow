## Decision

以条目而非段落中的“已立项”“已完成”字样判定状态：首先定位它声称的当前缺口，再核对权威主规范、归档变更和当前实现。完整已解决的条目删除；同一条目含剩余风险时改写为只描述该风险；未复现、没有触发条件的重构建议不继续占用 backlog。历史结果由 Git 和归档验证保存，不在 backlog 复制过程时间线。

特别检查 `inventory-all-crate-features` 的草稿归属：`reviews/draft-deltas` 不属于主规范。删除记录不得把平台未验证、性能无测量或对抗性竞态说成已解决。
