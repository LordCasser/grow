The existing mock classifier distinguishes untagged foreground requests by at least two tools. The fixture only exposed todo_write (context_recall is conditional), causing all three concurrent expectations to share one lane. Add the real read_file tool to the test fixture alongside todo_write; cross-turn coverage keeps its conditional context_recall as a third tool. Then select foreground matchers for tool/continuation responses. The blocked auxiliary response remains unreleased while the foreground must finish within 10 seconds, so the concurrency assertion is unchanged. Final full Linux CI is the regression authority; no timeout increase or ignored failure.
