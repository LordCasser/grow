## Why

Three Pager backlog lines no longer describe actionable debt: one geometry audit bullet contains only resolved findings and an unsupported catch-all, the pbpaste reference points to a change directory that has moved to the archive, and the coordination bullet repeats a resolved tail-only append audit beside the remaining risks.

## What Changes

- Remove the resolved geometry bullet.
- Link the pbpaste clause to its archived verification record.
- Keep only unresolved identity, panic, and passive-row lifecycle risks in the coordination bullet; the resolved tail-only audit remains in its archive.

## Capabilities

No behavior or contract changes. `.openspec.yaml` sets `skip_specs: true`; this is a narrow backlog maintenance change and adds no spec delta.

## Impact

- `openspec/backlog.md` only; no code or developer guide changes.
