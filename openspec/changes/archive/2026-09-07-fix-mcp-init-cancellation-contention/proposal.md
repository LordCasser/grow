# Why
InitGuard 的 try_lock 在其他线程持状态锁时失败，丢弃取消目标并留下 Initializing。唤醒等待者不能修复该状态，后续只会重复超时。前两项取消修复均不覆盖此路径。

# What Changes
使取消清理在短时状态锁竞争下确定完成；先核对全部客户端状态锁临界区，再将只保护同步状态的锁调整为同步互斥锁。握手和等待通知保持在锁外。

# Impact
只调整 McpClient 私有状态锁，不改 McpState 会话共享异步锁，不引入后台清理任务。现有依赖异步锁排队的两项回归需迁移为实际线程竞争与握手共享测试，保留原行为覆盖。
