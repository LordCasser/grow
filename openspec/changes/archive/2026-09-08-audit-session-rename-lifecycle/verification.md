# Verification
Scoped source reads on main; no running session deletion, rename, fault injection or build. Evidence is lock ordering and async check/write boundaries, not demonstrated disk corruption.
