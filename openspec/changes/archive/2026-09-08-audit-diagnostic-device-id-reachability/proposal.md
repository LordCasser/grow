## Why
Follow atomic cache callers after permission-cache repairs. Determine whether diagnostics device identification is actually enabled before expanding its implementation.

## What Changes
Read-only code/dependency audit and deletion candidate R25; no runtime behavior change, therefore no delta.
