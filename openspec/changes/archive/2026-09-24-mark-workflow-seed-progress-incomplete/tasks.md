## Implementation

- [x] Mark Timeline-seed Workflow restore usage incomplete in the shared resolver.
- [x] Add focused restore regressions for missing/invalid and valid sidecars.
- [x] Document the degraded progress boundary and remove the resolved backlog item.

## Verification

- [x] Run focused Workflow restore and budget tests.
- [x] Run `openspec validate --all --strict --no-interactive` and archive after review.
