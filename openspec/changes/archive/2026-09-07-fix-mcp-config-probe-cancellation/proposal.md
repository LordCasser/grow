# Why
恢复路径在配置探测时直接 await，生产探测会等待 mcp_state 异步锁。取消只覆盖退避和实际恢复动作，配置锁等待可能让调度或恢复任务无法及时结束并继续持有去重标记。

# What Changes
stdio/HTTP 的调度和循环配置探测均与取消 token 竞争，取消优先退出且不推送误导的 Disabled 状态。

# Impact
仅四处异步配置探测等待和取消回归，不改变配置来源、重试次数或传输。
