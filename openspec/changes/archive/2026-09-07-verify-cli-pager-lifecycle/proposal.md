## Why
分页器失败反馈、引号参数解析和重连后 transcript 自动重建均已独立验证，但当前 CLI 仍是这些修复之前的产物。需要重新验证组合根构建并留下可复查的二进制身份。

## What Changes
低磁盘配置构建 main 当前工作树 CLI，运行只读帮助和版本入口，记录结果、哈希及限制。仅工具验证，不修改行为契约，跳过 delta specs。

## Impact
不替换用户安装版本；不重跑无变化的全套测试。
