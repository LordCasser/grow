# Why
/export路径补全在UI线程同步read_dir；1000项上限按过滤隐藏文件后的items计数，隐藏项和读取错误不消耗预算，因此注释的目录扫描上限不成立。

# What Changes
在读取目录迭代器处限制最多1000项（包括隐藏项和错误），保留可见结果目录优先排序和最终100项上限。补充普通目录及可控迭代器回归。

# Impact
只限制补全扫描工作量，不改变导出内容、写入原子性或路径执行。预算之外的目录条目不保证出现在补全，可手工输入路径；同步慢盘延迟仍非本项解决。
