# Verification

- `cargo build --locked --offline -p cli --bin grow`: passed after the shared `CoordinationRow` compile error was fixed. The linker emitted a compact-unwind warning; the build completed.
- The PR loop contains 17 serial, full-path `--exact` invocations: 16 backlog scenarios from four integration targets and one real-`less` transcript restore case. `PAGER_BINARY` points to `target/debug/grow`; `less` is installed in the Ubuntu job. The paths were checked with each target's `--list` output before selecting them.
- Representative exact PTY cases passed: minimal Responses reasoning (1/1, 1.49s), config UI thinking toggle (1/1, 15.53s), scroll selection resize (1/1, 9.70s), queue send-now tip (1/1, 6.38s), and real `less` transcript restore (1/1, 8.87s).
- The first minimal reasoning run revealed that `minimal · /help` can appear behind the startup project picker. The prompt keys were consumed by the picker, so the mock received zero inference requests and its expectation stayed Pending. Selected fixtures now initialize their isolated sandbox home as a Git project; the corrected exact reasoning case passed. Mock provider seeding remains per fixture, and `seed_ui_config` appends its table without overwriting provider settings.
- `leader_two_clients_shared_session` was executed as an exact sample and failed during turn 2 after attaching the viewer and replaying turn 1. The app reported `turn terminal was not durably recorded: timeline event violates the causal fold: turn boundary … does not match active turn …`; `MOCKRESPONSE_T2` did not appear, and the test timed out after 120 seconds in `flows.rs`. The leader pair is excluded from this PR loop and retained as the only Pager backlog item for a separate Timeline/event-order investigation. No Timeline production semantics were changed.
- The five representative passes do not replace a full local run of all 17 selected cases. The workflow runs them serially in PR CI. Ordinary Cargo test targets continue to keep the cases ignored.
- `openspec validate add-pager-pty-pr-coverage --strict --no-interactive`: passed. `git diff --check`: passed. Workflow YAML parsed and the heredoc contains exactly 17 target/path rows with `PAGER_BINARY` and `less` setup.
