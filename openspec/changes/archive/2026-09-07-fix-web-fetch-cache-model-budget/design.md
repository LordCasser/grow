# Design
模型切换已更新 WebFetchClient.params，但缓存由克隆共享，直接返回旧输出会绕过预算。读取时克隆缓存值并释放锁，再复用 OverflowHandler 按当前参数处理完整文本。不能清空共享缓存解决问题：旧调用仍可写回大窗口结果。原缓存不写入裁剪结果或会话路径，保留大窗口调用和其他克隆的完整内容。

测试向实际客户端缓存注入完整文本，经公开 fetch 路径验证大窗口、缩小窗口、恢复文件及再次放大窗口；无需真实网络。
