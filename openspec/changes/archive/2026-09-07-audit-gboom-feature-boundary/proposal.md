## Why
隐藏入口 gboom 被相邻调试命令注释引用，但它实际是射击小游戏。需要区分非必要彩蛋和诊断功能，核对完整接入面，再提供可由用户确认的删除范围，不能因为它隐藏而假设未运行或直接移除。

## What Changes
审计命令、引擎、渲染和事件循环接入，增加 R15 可选小游戏删除候选。保留全部代码；无行为契约变化，跳过 delta specs。

## Impact
仅审计和临时候选清单。
