## Why
源状态从路径 metadata 获取身份和权限，再另行 open 读取内容。如果路径在两步间被原子替换，会把两个文件的数据合成一个快照，破坏后续确认和重验依据。

## What Changes
从同一已打开文件读取元数据与有界内容，保留非普通文件路径预检；新增真实文件替换测试。

## Impact
config managed_text 源快照，不改变格式、大小或路径解析规则。
