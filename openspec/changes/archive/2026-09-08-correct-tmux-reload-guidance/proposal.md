## Why
doctor预览、诊断和用户指南把detach/reattach当作source-file的替代方式。tmux服务器通常在客户端断开后仍然运行，重新连接不会自动读取改动的配置，用户按提示操作后问题仍存在。

## What Changes
说明运行中服务器需要显式重新加载配置；修复无法安全展示路径时的同类错误建议，并同步现有字符串快照。

## Impact
提示文字与用户指南，不执行tmux命令，不改变修复写入行为。
