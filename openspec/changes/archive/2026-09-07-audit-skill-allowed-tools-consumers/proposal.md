## Why
allowed-tools 的名称容易被理解为执行权限约束，但当前可见消费路径仅展示元数据。修改解析前必须核实真实能力，不能把未实现的权限限制当作既有功能修复。

## What Changes
记录消费者证据并明确用户说明中的元数据边界；不改变运行时行为。

## Capabilities
纯审计及说明，skip_specs=true。

## Impact
用户指南与 backlog；不新增权限机制，不删除字段。
