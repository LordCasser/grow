# Why
Slash MRU 后台使用无界mpsc完整快照队列，慢盘时旧快照持续占用内存并延迟最新历史落盘。线程启动或连接失败还会回退到UI同步写盘，违背后台写入目的。

# What Changes
只保留一个待写最新快照，另允许一个在途写入。用容量1的唤醒通道通知已有后台线程；完整快照覆盖待写快照。写线程不可用返回false，由控制器现有dirty处理在下次命令使用时重试，不执行同步文件IO。

# Impact
仅MRU写入调度；保持原子写入、全量快照、单线程顺序以及best-effort退出语义。不增加定时重试、退出flush、跨进程合并或通用调度框架。
