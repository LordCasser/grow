# Why
respawn_stdio 已检查配置 generation，但把“配置替换导致旧握手失效”作为普通 String 错误返回。若发生在最后一次重试，恢复循环仍推送失败/耗尽并按服务器名注销工具，可能影响新配置。

# What Changes
将配置替换结果与真实恢复失败区分，旧任务失效时静默结束，不推送失败、不重试、不删除工具；普通失败维持现有策略。

# Impact
stdio 恢复结果类型及其生产绑定/测试。HTTP 恢复不混入。
