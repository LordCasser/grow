Reuse PromptUsage and its existing model_usage map; no new wire entity or persisted ledger. Capture the catalog provider/model before each sampler call and use it for recording response usage, irrespective of provider response aliases. Render one shared text projection for modal and minimal mode. Overall cache-hit rate = sum(cache-read input) / sum(full input), not an average of model rates; output and cache creation are not numerator terms. Zero input and invalid cache > input yield N/A. Incomplete ledgers label rates as based on recorded usage. Totals are full input plus output; reasoning is already included in output. Keep the existing cost display and counting window.
