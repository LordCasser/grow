## Why
Input recorder 的文件导出已修复，需要继续核对采集、容量和脱敏边界，区分实际使用的诊断功能与没有生产消费者的遗留测试辅助函数，并纠正误导开发者的快捷键说明。

## What Changes
记录源码审计，修正快捷键注释；把 test-only 原始格式化函数作为 R13 删除候选，保留代码等待用户确认。无运行时契约改动，跳过 delta specs。

## Impact
仅注释、审计记录和临时候选清单；不改采集、字段、脱敏或快捷键。
