run_shell_child already stamps delegated_goal from the typed SubagentOwner and shares the usage window for every child. Keep root turn inference; for child inference require delegated_goal. Do not alter usage-window enforcement or snapshot validation. Update the earlier delegated test fixture to set the same flag as production and add a normal Task child /context turn with an active shared window and no Goal resource.
