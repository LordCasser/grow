## Why
Image recovery captures a relative timeout before asynchronous preparation, permitting provider work beyond the existing recovery deadline.

## What Changes
Bound auxiliary image-description provider polling by an absolute recovery deadline. Preserve durable Sideband finalization and non-lossy failure handling.
