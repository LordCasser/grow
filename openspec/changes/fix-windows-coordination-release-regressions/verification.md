# Verification

- Combined commit `24e930fa`: core regression and CLI [34480871337](https://github.com/LordCasser/grow/actions/runs/34480871337) passed (12,418 tests, 14 existing ignored); Windows storage [34480875421](https://github.com/LordCasser/grow/actions/runs/34480875421) passed. Linux/macOS complete process scenarios passed in targeted [34480880214](https://github.com/LordCasser/grow/actions/runs/34480880214). Windows now initializes and discovers peers, proving the debug main-stack correction, but its foreground does not reach the model. A cleanup warning alone does not establish the cause; the harness now surfaces a completed foreground RPC and recent fixture notifications on failure. Logs: `/tmp/grow-v2.1.6-merged-core-ci.log`, `/tmp/grow-v2.1.6-merged-windows-process.log`.

- Failing Windows run: [34469589238](https://github.com/LordCasser/grow/actions/runs/34469589238), job 102846210811. 28 coordination cases passed and 9 failed: seven actor-fixture path failures, held-reader manifest replacement, and live sideband observation. Full log retained at `/tmp/grow-v2.1.6-windows-coordination-failed.log`.
- After correction, local full shell regression passed: 3,788 passed, 0 failed, 3 ignored. Command: `CARGO_INCREMENTAL=0 RUST_MIN_STACK=16777216 cargo test --locked -p shell --lib -- --test-threads=4`. Log: `/tmp/grow-v2.1.6-windows-fix-shell.log`.
- Existing manifest regression now additionally distinguishes the old reader's heartbeat from the new canonical heartbeat, while retaining token validation. The session observation regression additionally checks that observation does not populate the writer capability cache. All original scenarios remain present.
- `git diff --check` and OpenSpec strict validation passed (18 current items).
- Commit `162ba0ba`: Linux core and CLI [34471679559](https://github.com/LordCasser/grow/actions/runs/34471679559), Windows session storage [34471686294](https://github.com/LordCasser/grow/actions/runs/34471686294), and Linux/macOS complete coordination jobs in [34471683270](https://github.com/LordCasser/grow/actions/runs/34471683270) passed.
- The Windows job in that coordination run passed all Shell coordination cases, including the former nine failures, then found a separate PTY test-harness compile error: `process_has_exited_without_reap` is exported only on Unix but imported unconditionally. The runtime call and process-observation tests already have `cfg(unix)`; the import now uses the same condition. Log: `/tmp/grow-v2.1.6-windows-pager-failed.log`.
- Coordination CI now retains Cargo caches after failure, matching the established core/storage workflows, to avoid repeatedly rebuilding dependencies while resolving platform validation failures.
- Final Windows Pager/build/independent-process results are pending. The runtime correction is unchanged from the successful core, storage and Linux/macOS runs. This change stays unarchived until required Windows results pass.
- Commit `e2fb35a6`: Linux/macOS coordination passed in [34473435938](https://github.com/LordCasser/grow/actions/runs/34473435938), and Windows passed the shell regressions, then found three unresolved `symlink` calls in the disk-usage test fixture. Windows-specific file/directory helpers already existed but were unused. The calls now select those helpers, with Unix aliases preserving the original behavior and every assertion. Log: `/tmp/grow-v2.1.6-windows-pager-final-failed.log`.
- Commit `5a248592`: Linux/macOS complete coordination passed in [34475743582](https://github.com/LordCasser/grow/actions/runs/34475743582). Windows passed every Shell/Pager/Minimal library step and CLI compilation, then its first fixture process closed stdout during initialization. Stderr was lost when the fixture directory was removed, so the cause is not established. The harness now emits bounded stderr and exit diagnostics before cleanup; a targeted CLI/process rerun will preserve these unchanged library passes. Log: `/tmp/grow-v2.1.6-windows-process-failed.log`.
- Commit `ec512363`, targeted `process_only=true` run [34479024751](https://github.com/LordCasser/grow/actions/runs/34479024751): Windows exited with 3221225725 (`0xC00000FD`); stderr reports `thread 'main' ... has overflowed its stack`. This establishes the startup failure as debug main-thread stack exhaustion. The CI-only linker allowance and native distribution process smoke gates address the validation boundary without reshaping production code. Log: `/tmp/grow-v2.1.6-windows-startup-diagnostic.log`.

- Commit `3516e69e`, targeted [34482340006](https://github.com/LordCasser/grow/actions/runs/34482340006): Linux/macOS passed all 10 process groups. Windows foreground RPC now reveals `input artifact write failed: The filename or extension is too long. (0x800700CE)` before provider dispatch. Separate cleanup stderr reports `ERROR_DIRECTORY` (267) during session enumeration. Source review confirms raw DOS rename encoding and the Windows/Unix non-directory classification asymmetry. Log: `/tmp/grow-v2.1.6-windows-foreground-diagnostic.log`. The following correction retains the default temporary profile and expands Windows storage to six exact boundary regressions; results pending.

- Candidate `a12e5b35` failed Windows [storage 34483776526](https://github.com/LordCasser/grow/actions/runs/34483776526) (reopen NotFound) and [process 34483781086](https://github.com/LordCasser/grow/actions/runs/34483781086) (session/new ERROR_INVALID_NAME); Linux/macOS process cases passed. These failures invalidate the initial long-path correction. Rust 1.93.1's native rename includes a trailing NUL in the allocation while excluding it from FileNameLength; the repository's two handle-rename buffers did not explicitly reserve that terminator. An isolated ctypes probe compares counted-only and terminated buffers with an in-allocation poison tail for short/long file/directory names and both information classes. This diagnostic performs no Grow or user-storage operations.

- [Isolated probe 34485215041](https://github.com/LordCasser/grow/actions/runs/34485215041) passed its eight terminated-buffer assertions. All eight nonterminated short/long file/directory cases returned success but published `POISON` as a spurious suffix, proving an actual counted-buffer overread by both Win32 information classes. Log: `/tmp/grow-v2.1.6-rename-api-probe.log`. Both production rename buffers now explicitly retain the NUL; Windows storage runs eight exact regressions, including filename alignments and long held-reader manifest replacement.

- Candidate `2761a030`, [Windows storage 34485532619](https://github.com/LordCasser/grow/actions/runs/34485532619): creation/reopen, long-root create/reload/fork, invalid-entry scan and no-replace collision all passed. The fifth pre-existing capability test incorrectly used a writer-style target open while the published DELETE-capable handle remained live (Error 32). It now uses the established observation capability, preserves exact identity/contents checks, then verifies ordinary writer reopen after both live capabilities close. Log: `/tmp/grow-v2.1.6-terminated-storage.log`.

- Candidate `2761a030`, full [coordination 34485537209](https://github.com/LordCasser/grow/actions/runs/34485537209): Windows passed every library step and CLI compilation, then its foreground reached the model but the first inquiry did not. The fixture did not preserve the completed RPC result, so the exact inquiry failure is not established by the timeout. Retain the most recent 20 fixture RPC replies (bounded to 16 KiB when printed on failure) alongside existing stderr/notices. Log: `/tmp/grow-v2.1.6-terminated-process.log`.

- Combined candidate `afc9a481`: all eight exact Windows storage cases passed in [34486767916](https://github.com/LordCasser/grow/actions/runs/34486767916), including the former observation fixture failure, long payload round-trip/collision preservation and both rename alignment paths. Log: `/tmp/grow-v2.1.6-final-windows-storage.log`. Linux/macOS each passed all ten process groups in [34487126857](https://github.com/LordCasser/grow/actions/runs/34487126857); Windows result pending.
