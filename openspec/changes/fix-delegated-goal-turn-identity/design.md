Turn identity already selects the active shared Goal id and uses a matching root tracker revision. When that revision is absent, consult the existing GoalContextSnapshotResource and accept only the same goal_id. Never synthesize revision 1, omit the owner to bypass validation, create a local child Goal tracker, or weaken Timeline validation. Root and explicit Goal-continuation identities remain unchanged.
