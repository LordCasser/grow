Before general active-run reaping, inspect a requested resume run. If it is still Active, preserve NotResumable. If its tracker is resumable and an ActiveRun watcher still exists, await its existing done receiver and propagate failure before any new Timeline admission. Re-read the tracker through normal resume validation after acknowledgement. A test blocks terminal manifest acknowledgement, attempts resume, and verifies it remains pending then completes from its journal after release.
