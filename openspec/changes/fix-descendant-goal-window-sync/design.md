The GoalUsageWindow is shared across descendants; each child owns its local tracker but not the shared lifecycle. Reuse SessionStartupHints.is_subagent, already the accounting and shutdown ownership discriminator. Guard sync_goal_usage_window and the direct startup conflict sync. Do not weaken begin_model_attempt checks or rewrite user session data. Tests exercise child synchronization against active, exhausted, incomplete and inactive windows and verify root admission/ownership.
