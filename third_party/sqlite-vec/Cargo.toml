# VENDORING NOTES
# Upstream: https://github.com/asg017/sqlite-vec v0.1.10-alpha.4
# (MIT OR Apache-2.0), commit 04d28bd21773981e2d266bbf6aa4efbd011eb4f6.
# Base: crates.io package sqlite-vec 0.1.10-alpha.4, checksum
# 6436dbf1d78c5015f9ae21822dbaa32368039d6b07708874a432c33c0548e47d.
# Why vendored: every published package containing the upstream musl typedef
# fix (0.1.10-alpha.2 through alpha.4) omits four C implementation files
# included by sqlite-vec.c. The Git repository contains them but has no root
# Cargo package that can be consumed unchanged as a submodule/path dependency.
# Local additions:
#   sqlite-vec-diskann.c, sqlite-vec-ivf-kmeans.c, sqlite-vec-ivf.c, and
#   sqlite-vec-rescore.c: byte-identical to the files at the upstream tag above.
# No upstream C or Rust source is modified.
# Upgrade checklist: prefer returning to the exact crates.io dependency once a
# release both contains the musl fix and packages every included C file. Until
# then, re-copy the published crate and the missing files from the same tag.
#
# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "sqlite-vec"
version = "0.1.10-alpha.4"
authors = ["Alex Garcia <alexsebastian.garcia@gmail.com>"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "FFI bindings to the sqlite-vec SQLite extension"
homepage = "https://github.com/asg017/sqlite-vec"
readme = false
keywords = [
    "sqlite",
    "sqlite-extension",
]
license = "MIT/Apache-2.0"
repository = "https://github.com/asg017/sqlite-vec"

[lib]
name = "sqlite_vec"
path = "src/lib.rs"

[dependencies]

[dev-dependencies.rusqlite]
version = "0.31.0"

[build-dependencies.cc]
version = "1.0"
