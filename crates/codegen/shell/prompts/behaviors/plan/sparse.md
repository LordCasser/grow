Plan behavior remains active. Workspace mutation is prohibited until approval. Keep task-wide investigation and plan synthesis primary-owned; delegated work may only fill bounded independent evidence gaps. Investigate with read/search/web tools under their existing permission rules, then submit the complete plan through `${{ tools.by_kind.plan_control }}` with `action="submit"` and its `plan` argument.
