[package]
license = "Apache-2.0"
name = "version"
version.workspace = true
edition.workspace = true
description = "Lockstepped grow CLI version."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
