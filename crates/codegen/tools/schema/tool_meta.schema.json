{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CanonicalToolMeta",
  "description": "The canonical tool-identity envelope, attached to a tool-call event `_meta`\nas one nested object under [`TOOL_META_KEY`].\n\n```json\n\"grow/tool\": {\n  \"version\": 3,\n  \"name\": \"read_file\",\n  \"kind\": \"read\",\n  \"namespace\": \"grow\",\n  \"label\": \"Read\",\n  \"max_access\": \"read\",\n  \"input\": { \"path\": \"...\" }\n}\n```\n\nConsumer contract:\n- **`label`** is the cross-harness grouping/display key: equivalent tools\n  share it (grow `read_file` → `\"Read\"`).\n- **`kind`** is a finer discriminator (`metadata.kind()`), *not* guaranteed\n  equal for equivalent ops across harnesses (listing is `list` in one\n  toolset, `list_dir` in another); prefer `label` to join.\n- **`name`** is the harness-specific model-facing name; for diagnostics.\n  For harness-initiated events (e.g. the `bash_mode` marker), `raw_input`\n  is not guaranteed to match `name`'s schema.\n- **`input`** is a canonical *projection*, not a mirror: cross-harness keys\n  only, so some raw fields are intentionally dropped (e.g. grep flags,\n  `replace_all`), and bulky payload\n  fields (edit `old_string`/`new_string`, full write contents) are never\n  projected — read them from `raw_input`. It is omitted entirely\n  when no stable shape exists (MCP / dynamic / out-of-scope). When a field or\n  the whole dict is absent, fall back to `raw_input` on this or an earlier\n  update for the same `tool_call_id` (some updates, e.g. a parse failure,\n  carry neither and rely on the merge below).\n- **Lifecycle:** updates for one call share a `tool_call_id` — merge across\n  them (last write wins); `input` may arrive on a later update.\n- **Versioning:** `kind`, `namespace`, and the envelope fields are closed.\n  Any vocabulary or shape change bumps `version`, forcing typed consumers to\n  update rather than silently reclassifying a tool.",
  "type": "object",
  "properties": {
    "version": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "name": {
      "type": "string"
    },
    "kind": {
      "$ref": "#/definitions/ToolKind"
    },
    "namespace": {
      "$ref": "#/definitions/ToolNamespace"
    },
    "label": {
      "type": "string"
    },
    "max_access": {
      "$ref": "#/definitions/ToolAccess"
    },
    "input": true
  },
  "additionalProperties": false,
  "required": [
    "version",
    "name",
    "kind",
    "namespace",
    "label",
    "max_access"
  ],
  "definitions": {
    "ToolKind": {
      "description": "Categorizes what a tool does at a high level.\n\nSerializes as snake_case strings (e.g. `\"read\"`, `\"list_dir\"`, `\"web_fetch\"`).\n`Other` is an explicit category for tools that do not fit elsewhere. The\nwire vocabulary is closed: unknown values are rejected instead of silently\nchanging capability and permission classification.",
      "type": "string",
      "enum": [
        "read",
        "edit",
        "delete",
        "list_dir",
        "write",
        "move",
        "search",
        "lsp",
        "execute",
        "plan",
        "web_fetch",
        "background_task_action",
        "kill_task_action",
        "list",
        "skill",
        "memory_search",
        "memory_get",
        "context_recall",
        "task",
        "plan_control",
        "ask_user",
        "deploy_app",
        "search_tool",
        "use_tool",
        "monitor",
        "goal_read",
        "goal_lifecycle_update",
        "workflow",
        "capability_request",
        "other"
      ]
    },
    "ToolNamespace": {
      "description": "The toolset a tool belongs to.\n\nSerializes to one canonical snake_case vocabulary (`grow`, `mcp`, …) for\nthe tool `_meta` wire contract. The `Display` impl remains PascalCase for\nqualified runtime ids (for example `\"Grow:read_file\"`); only the serde form\ngoes on the wire.",
      "type": "string",
      "enum": [
        "grow",
        "grow_concise",
        "grow_hashline",
        "mcp"
      ]
    },
    "ToolAccess": {
      "description": "Coarse read/write/execute authority required by a tool call.\n\n`Read` observes state, `Write` changes durable state or emits externally,\nand `Execute` starts or controls an active execution unit. Combined tools\ndeclare the union. This vocabulary is intentionally closed and replaces the\nold read/write-only `ToolScope` classification.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "read",
            "write",
            "execute",
            "read_write",
            "read_execute",
            "write_execute",
            "all"
          ]
        },
        {
          "description": "Pure control-plane operation with no external observation or emission.",
          "type": "string",
          "const": "none"
        }
      ]
    }
  }
}
