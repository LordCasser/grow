[package]
license = "Apache-2.0"
name = "xai-file-utils"
version = "0.1.0"
edition.workspace = true
description = "General utilities: SHA-256 hashing and workspace classification"

[dependencies]
dirs = { workspace = true }
sha2 = { workspace = true, features = ["force-soft"] }

[dev-dependencies]
tempfile = { workspace = true }

[features]
default-bazel = []

[lints]
workspace = true
